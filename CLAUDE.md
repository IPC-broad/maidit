@AGENTS.md

## Security rules

Never include actual API key values, service role keys, or secret tokens in any suggested commands or output text. When showing example curl commands or similar, always use placeholders like `YOUR_KEY_HERE` instead of actual values.

## UI CHANGE RULES — STRICT
1. NEVER redesign or restructure existing pages unless explicitly asked
2. NEVER change the layout, flow, or visual design of working pages
3. Only change what is specifically requested — nothing more
4. If a command could affect UI layout, ASK first before proceeding
5. Kasambahay offer review page (/offer/review/[id]) must show:
   - Full offer details with individual agree/disagree toggles per item
   - "Tingnan ang buong offer" flow intact
   - Do NOT simplify or restructure this page
